<% if (can? :add_sale_cash, Document) %>
  <%= link_to 'إضافة مستند جديد', add_sale_cash_documents_path,class: "btn btn-primary" %>
  <hr>
<% end %>

<%= render 'index' %>

<% if (can? :add_sale_cash, Document) %>
  <div class="card-footer">
    <%= link_to 'إضافة مستند جديد', add_sale_cash_documents_path,class: "btn btn-primary" %>
  </div>
<% end %>
