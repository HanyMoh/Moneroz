<% if (can? :add_selling_futures, Document) %>
  <%= link_to 'Add New', add_selling_futures_documents_path,class: "btn btn-primary" %>
  <hr>
<% end %>

<%= render 'index' %>

<% if (can? :add_selling_futures, Document) %>
  <div class="card-footer">
    <%= link_to 'Add New', add_selling_futures_documents_path,class: "btn btn-primary" %>
  </div>
<% end %>
