<% if (can? :add_barcode, Document) %>
  <%= link_to 'Add New', add_barcode_documents_path,class: "btn btn-primary" %>
  <hr>
<% end %>

<%= render 'shared_doc/index' %>

<% if (can? :add_barcode, Document) %>
  <div class="card-footer">
    <%= link_to 'Add New', add_barcode_documents_path,class: "btn btn-primary" %>
  </div>
<% end %>
