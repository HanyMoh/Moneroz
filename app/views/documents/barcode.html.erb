<% if (can? :add_barcode, Document) %>
  <%= link_to 'إضافة مستند جديد', add_barcode_documents_path,class: "btn btn-primary" %>
  <hr>
<% end %>

<%= render 'index' %>

<% if (can? :add_barcode, Document) %>
  <div class="card-footer">
    <%= link_to 'إضافة مستند جديد', add_barcode_documents_path,class: "btn btn-primary" %>
  </div>
<% end %>
