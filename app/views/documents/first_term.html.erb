<% if (can? :add_first_term, Document) %>
  <%= link_to 'إضافة مستند جديد', add_first_term_documents_path,class: "btn btn-primary" %>
  <hr>
<% end %>

<%= render 'index' %>

<% if (can? :add_first_term, Document) %>
  <div class="card-footer">
    <%= link_to 'إضافة مستند جديد', add_first_term_documents_path,class: "btn btn-primary" %>
  </div>
<% end %>
